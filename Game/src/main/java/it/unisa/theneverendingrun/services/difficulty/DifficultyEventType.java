package it.unisa.theneverendingrun.services.difficulty;

/**
 *
 * An enumeration containing all the topic related to {@link DifficultyMetersListener}
 */
public enum DifficultyEventType {

    /**
     *
     * A topic that updates each time the {@link DifficultyMetersListener} difficulty variable changes
     */
    LEVEL_CHANGED,
    ;
}
